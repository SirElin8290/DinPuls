const assert=require("assert"),fs=require("fs"),path=require("path"),root=path.resolve(__dirname,"..");
const data=JSON.parse(fs.readFileSync(path.join(root,"data/family-places.json"),"utf8")),engine=fs.readFileSync(path.join(root,"family-engine.js"),"utf8"),page=fs.readFileSync(path.join(root,"family-page.js"),"utf8"),html=fs.readFileSync(path.join(root,"skola-familj.html"),"utf8"),css=fs.readFileSync(path.join(root,"school-family.css"),"utf8"),home=fs.readFileSync(path.join(root,"components/primary-cards.html"),"utf8");
assert(Array.isArray(data.categories)&&data.categories.length>=15,"Familjekategorier ska ligga i data/config");
const expectedMunicipalities=["Åmål","Årjäng","Bengtsfors","Mellerud","Arvika","Grums","Säffle","Dals-Ed","Eda","Filipstad","Forshaga","Färgelanda","Hagfors","Hammarö","Karlstad","Kil","Kristinehamn","Munkfors","Storfors","Sunne","Torsby"];
assert.deepStrictEqual(Object.keys(data.municipalities),expectedMunicipalities,"Familjemotorn ska ha separata dataset för samtliga 21 lanseringskommuner");
const amal=data.municipalities["Åmål"];assert(amal.places.length===9,"Åmåls referensdataset ska fortsatt innehålla nio verifierade platser");
const categoryIds=new Set(data.categories.map(item=>item.id)),ids=new Set();
let totalPlaces=0;
for(const municipalityName of expectedMunicipalities){const municipality=data.municipalities[municipalityName];assert(/^\d{4}$/.test(municipality.municipalityCode),`${municipalityName}: kommunkod saknas`);assert(Number.isFinite(municipality.mapCenter?.latitude)&&Number.isFinite(municipality.mapCenter?.longitude),`${municipalityName}: kartcentrum saknas`);assert(municipality.places.length>0,`${municipalityName}: separat familjedataset saknas`);for(const place of municipality.places){totalPlaces++;assert(!ids.has(place.id),`Duplicerat plats-id: ${place.id}`);ids.add(place.id);assert.strictEqual(place.municipality,municipalityName,`${place.name}: fel kommun`);assert(Number.isFinite(place.coordinates?.latitude)&&Number.isFinite(place.coordinates?.longitude),`${place.name}: koordinater saknas`);assert(place.coordinates.latitude>=55&&place.coordinates.latitude<=70&&place.coordinates.longitude>=10&&place.coordinates.longitude<=25,`${place.name}: koordinater ligger utanför Sverige`);assert(place.categories.length&&place.categories.every(id=>categoryIds.has(id)),`${place.name}: okänd kategori`);assert(place.source?.url===place.primaryUrl&&/^https:\/\//.test(place.source.url),`${place.name}: spårbar primärkälla saknas`);assert(/^\d{4}-\d{2}-\d{2}$/.test(place.verifiedAt),`${place.name}: verifieringsdatum saknas`);assert.strictEqual(place.verificationStatus,"verified",`${place.name}: ej verifierad`)}}
assert.strictEqual(totalPlaces,57,"Familjedatasetets verifierade omfattning har ändrats oväntat");
assert([...ids].length===totalPlaces,"Varje fysisk plats ska bara finnas en gång");
assert(Object.values(data.municipalities).flatMap(item=>item.places).some(place=>place.categories.length>=4),"En fysisk plats ska kunna ha flera kategorier utan duplicering");
assert(engine.includes("payload.municipalities?.[name]||null"),"Familjemotorn måste slå upp vald kommun utan Åmål-datafallback");
assert(engine.includes("new URLSearchParams(location.search).get(\"kommun\")"),"Explicit kommunparameter måste prioriteras");
assert(page.includes('select.add(new Option("Visa allt","all"))')&&page.includes('place.categories.includes(filter)'),"Visa allt och kategorifilter saknas");
assert(page.includes('marker.on("click",()=>focusPlace(place.id,true))')&&page.includes('data-focus-place'),"Pins och platskort är inte sammankopplade");
assert(html.includes('id="family-map"')&&html.includes('id="family-category"')&&html.includes('SKOLA-FAMILJ-03'),"Familj-UI eller annonsplats saknas");
assert(home.includes('id="family-home-content"')&&home.includes("Vad kan vi göra idag?"),"Startsidans Familj-del saknas");
assert(css.includes(".strategic-ad-slot[hidden]{display:none!important}")&&css.includes(".family-map-layout"),"0px-annonsbeteende eller responsiv Familj-layout saknas");
console.log(`✓ Familj: generell motor, 21/21 separata kommundataset och ${totalPlaces} verifierade platser; multitaggar, filter, karta och pins ↔ kort verifierade`);
